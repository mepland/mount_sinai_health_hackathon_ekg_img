from setuptools import setup

setup(name='common_code',
      version='0.0',
      description='common_code',
      url='private',
      author='Matthew Epland',
      author_email='matthew.epland@duke.edu',
      license='',
      packages=['common_code'],
      zip_safe=False)
